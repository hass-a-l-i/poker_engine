[build-system]
requires = ["setuptools>=61.0"]
#requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "poker-engine"
version = "0.1.0"
authors = [{name = "Hassan"}]
readme = "README.md"
requires-python = ">=3.10"
description = "Texas hold-em ppker engine"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
where = ["src"]